version: '2'
services:
  frpc-docker:
    build: ../.
    image: frp:latest
    container_name: frpc-docker
    hostname: frpc-docker
    volumes:
      - "./frpc.ini:/etc/frp/frpc.ini"
    environment:
      - SERVER_IP
      - PASSWORD
      - FRP_MODE=frpc